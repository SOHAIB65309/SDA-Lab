{
  "name": "shopease",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build -w client",
    "vercel-build": "npm install && npm run build -w client",
    "server": "npm start -w server",
    "client": "npm start -w client",
    "kill-port": "npx kill-port 9999"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.7.3",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
